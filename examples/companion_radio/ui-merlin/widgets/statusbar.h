// MERLIN UI — the 14 px strip that always burns (design rule 4).
#pragma once

#include "../gfx.h"
#include "../model.h"

namespace merlin {
namespace statusbar {

void render(Gfx &g, const StatusView &s);

} // namespace statusbar
} // namespace merlin
